import { useEffect, useState } from "react"; import toast from "react-hot-toast"; import api from "../../services/api";
export default function Skills(){ const [items,setItems]=useState([]); const [form,setForm]=useState({"name": "", "category": "frontend", "level": "intermediate"}); const [editing,setEditing]=useState(null); const load=()=>api.get('/skills/admin').then(r=>setItems(r.data.data)); useEffect(()=>{load()},[]); const save=async(e)=>{e.preventDefault(); try{ if(editing) await api.put(`${'/skills'}/${editing}`, form); else await api.post('/skills', form); toast.success('Saved'); setForm({"name": "", "category": "frontend", "level": "intermediate"}); setEditing(null); load(); }catch(err){toast.error(err.response?.data?.message||'Save failed')} }; const edit=(x)=>{setEditing(x._id); setForm({...{"name": "", "category": "frontend", "level": "intermediate"},...x})}; const del=async(id)=>{ if(!confirm('Delete this item?')) return; await api.delete(`${'/skills'}/${id}`); load();}; return <div><h1 className="text-3xl font-black mb-6">Skills</h1><form onSubmit={save} className="glass rounded-3xl p-5 grid gap-4 mb-8"><input className="input" placeholder="name" value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})} /><input className="input" placeholder="category" value={form.category||''} onChange={e=>setForm({...form,category:e.target.value})} /><input className="input" placeholder="level" value={form.level||''} onChange={e=>setForm({...form,level:e.target.value})} /><button className="btn btn-primary w-fit">{editing?'Update':'Create'}</button></form><div className="grid gap-3">{items.map(x=><div key={x._id} className="glass rounded-2xl p-4 flex justify-between gap-4"><div><p className="font-bold">{x.title||x.name||x.company||x.institution}</p><p className="text-slate-400 text-sm">{x.category||x.position||x.degree||x.shortDescription}</p></div><div className="flex gap-2"><button onClick={()=>edit(x)} className="btn btn-ghost">Edit</button><button onClick={()=>del(x._id)} className="btn btn-ghost">Delete</button></div></div>)}</div></div> }
